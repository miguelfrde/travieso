{
  "name": "travieso",
  "description": "Show Travis CI build jobs on Github.",
  "repository": "https://github.com/wizeline/travieso",
  "logo": "https://github.com/wizeline/travieso/blob/master/travieso.png",
  "keywords": ["python", "flask", "github", "travis", "ci", "pull requests", "status"],
  "env": {
    "GITHUB_TOKEN": {
      "description": "A token generated for the account which will update the status of your commits.",
      "required": true
    },
    "TRAVIS_TOKEN": {
      "description": "Travis token for the user that activated the repo on Travis to authenticate the webhook",
      "required": true
    }
  }
}
